# Findings gitleaks has already reported and a human has already judged.
#
# Each line is an exact fingerprint — commit, file, rule, line. It exempts that one occurrence and
# nothing else: the same file at a different line, or the same string in a later commit, has a different
# fingerprint and is reported again. That precision is why this file is the right mechanism and a config
# allowlist was not. An allowlist keyed on the path would have exempted `_test.go` entirely, because
# gitleaks ORs its path and regex conditions rather than ANDing them — so a real credential pasted into
# a test file would have gone unreported. I checked, by pasting one.
#
# Both entries below are the test harness's bearer tokens, written as string literals in commits that
# are already published. The current tree no longer contains them: harnessCredential() builds them
# instead, so nothing new can add a literal here. These two lines exist only because history cannot be
# edited without rewriting every commit after them, which is not a trade worth making for a fixture.
#
# Before adding a line here: the question is not "is this finding annoying" but "has somebody looked at
# this exact occurrence and confirmed it is not a credential". If the answer is no, it does not go in.

403be9a144a849562b0a77bc72c56f448b945aea:internal/server/server_test.go:generic-api-key:95
7c2f084e7763670100f72ea16f5ecb1fbffbc1e1:internal/server/server_test.go:generic-api-key:71

# The third is the same story with a different fixture: a PEM header over the base64 of the word
# "openssh", written out so that the secret-shape detector the provisioning templates once had could
# find something. Looked at, and not a key. The current tree no longer contains it, nor the package
# that read it, so this line covers one published commit and cannot be joined by another.
7e6f9ebb9fbf5e7282a95ac7f6e6be7c59d28b47:internal/provision/provision_test.go:private-key:81

# The next two are one password, in two files, in a commit that documents the first-start output.
#
# It was a real password: `hostseal-server serve` generates one when the database holds no accounts, and
# the example block was pasted from an actual run rather than invented. It belonged to a throwaway
# PostgreSQL database in a development container, which has since been dropped; it never reached a
# machine anybody else can connect to, and there is no account left for it to open. gitleaks was right
# to report it — a generated password has the entropy of a credential because it is one — and the answer
# to a real finding is to look at the occurrence, which is what this comment is.
#
# The current tree carries a placeholder instead, so nothing new can add a literal here; these two lines
# exist only because the commit is published and rewriting a branch under review to bury a dead password
# is the more disruptive of the two mistakes.
17b440646b16d3a9ba4bee27516b49c734486459:docs/INSTALL.md:generic-api-key:85
17b440646b16d3a9ba4bee27516b49c734486459:deploy/README.md:generic-api-key:52
